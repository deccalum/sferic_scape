# Output directory for generated JSON/WAV (dev mode groups under OUT_DIR/<stem>/).
SFERIC_OUT_DIR=data/out

# Dev-mode input — a WAV file (or, later, a directory for batch analysis).
SFERIC_INPUT=data/wav/freesound/616992E.wav

# Synthesis sample rate (Hz).
SFERIC_SAMPLE_RATE=48000
